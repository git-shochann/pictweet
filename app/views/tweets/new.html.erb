<div class="contents row">
  <div class="container">
    <%= form_with(model: @tweet, local: true) do |form| %>
      <h3>
        投稿する
      </h3>
    <%= render partial: "form", locals: { form: form } %>
    <% end %>
  </div>
</div>


<%# form_withの中では、formという変数を利用しているので、locals: { form: form }という記述になっていることに注意 eachメソッドのformを部分テンプレート先のformで使用する為、ここで変数化 %>